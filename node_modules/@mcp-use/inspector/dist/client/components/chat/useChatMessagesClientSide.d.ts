import type { McpServer } from "mcp-use/react";
import type { PromptResult } from "../../hooks/useMCPPrompts";
import type { LLMConfig, Message, MessageAttachment } from "./types";
type MCPConnection = McpServer;
interface WidgetModelContext {
    content?: Array<{
        type: string;
        text: string;
    }>;
    structuredContent?: Record<string, unknown>;
}
interface UseChatMessagesClientSideProps {
    connection: MCPConnection;
    llmConfig: LLMConfig | null;
    isConnected: boolean;
    readResource?: (uri: string) => Promise<any>;
    widgetModelContexts?: Map<string, WidgetModelContext | undefined>;
    disabledTools?: Set<string>;
}
export declare function useChatMessagesClientSide({ connection, llmConfig, isConnected, readResource, widgetModelContexts, disabledTools, }: UseChatMessagesClientSideProps): {
    messages: Message[];
    isLoading: boolean;
    attachments: MessageAttachment[];
    sendMessage: (userInput: string, promptResults: PromptResult[]) => Promise<void>;
    clearMessages: () => void;
    setMessages: import("react").Dispatch<import("react").SetStateAction<Message[]>>;
    stop: () => void;
    addAttachment: (file: File) => Promise<void>;
    removeAttachment: (index: number) => void;
    clearAttachments: () => void;
};
export {};
//# sourceMappingURL=useChatMessagesClientSide.d.ts.map