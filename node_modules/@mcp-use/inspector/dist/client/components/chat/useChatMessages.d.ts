import type { PromptResult } from "../../hooks/useMCPPrompts";
import type { AuthConfig, LLMConfig, Message, MessageAttachment } from "./types";
interface WidgetModelContext {
    content?: Array<{
        type: string;
        text: string;
    }>;
    structuredContent?: Record<string, unknown>;
}
interface UseChatMessagesProps {
    mcpServerUrl: string;
    llmConfig: LLMConfig | null;
    authConfig: AuthConfig | null;
    isConnected: boolean;
    /** Custom API endpoint URL for chat streaming. Defaults to "/inspector/api/chat/stream". */
    chatApiUrl?: string;
    /** Active widget model contexts to inject into the LLM conversation */
    widgetModelContexts?: Map<string, WidgetModelContext | undefined>;
}
export declare function useChatMessages({ mcpServerUrl, llmConfig, authConfig, isConnected, chatApiUrl, widgetModelContexts, }: UseChatMessagesProps): {
    messages: Message[];
    isLoading: boolean;
    attachments: MessageAttachment[];
    sendMessage: (userInput: string, promptResults: PromptResult[]) => Promise<void>;
    clearMessages: () => void;
    setMessages: import("react").Dispatch<import("react").SetStateAction<Message[]>>;
    stop: () => void;
    addAttachment: (file: File) => Promise<void>;
    removeAttachment: (index: number) => void;
    clearAttachments: () => void;
};
export {};
//# sourceMappingURL=useChatMessages.d.ts.map