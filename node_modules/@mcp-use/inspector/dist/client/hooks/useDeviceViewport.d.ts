/**
 * Hook for calculating device viewport dimensions
 */
import type { DeviceType } from "../context/WidgetDebugContext";
export interface ViewportDimensions {
    maxWidth: number;
    maxHeight: number;
}
export interface CustomViewport {
    width: number;
    height: number;
}
/**
 * Calculate viewport dimensions based on device type
 */
export declare function useDeviceViewport(deviceType: DeviceType, customViewport: CustomViewport): ViewportDimensions;
//# sourceMappingURL=useDeviceViewport.d.ts.map